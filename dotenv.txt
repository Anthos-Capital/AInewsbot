# AInewsbot Environment Variables
# Copy this file to .env and add your API keys

# REQUIRED - Core functionality
OPENAI_API_KEY=''

# REQUIRED - News data source
NEWSAPI_API_KEY=''

# ===== EMAIL DELIVERY CONFIGURATION =====
# Method: 'ses', 'gmail', or 'file' (default: ses)
# - 'ses': AWS Simple Email Service (recommended for production)
# - 'gmail': Legacy Gmail SMTP (requires app password)
# - 'file': Save to local file (for testing/development)
EMAIL_DELIVERY='ses'

# AWS SES Configuration (if using EMAIL_DELIVERY=ses)
# AWS credentials can be provided via:
# 1. Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
# 2. IAM roles (when running on EC2/Lambda)
# 3. AWS CLI profile (~/.aws/credentials)
AWS_REGION='us-east-1'
SES_SENDER_EMAIL='ainews@yourdomain.com'  # Must be verified in SES
SES_RECIPIENT_EMAIL='team@yourdomain.com'  # Where to send newsletters

# SST Configuration (for infrastructure deployment)
SES_PROD_SENDER='ainews@yourdomain.com'  # Production sender
SES_DEV_SENDER='ainews-dev@yourdomain.com'  # Development sender

# Legacy Gmail (if using EMAIL_DELIVERY=gmail)
# Note: Requires 2FA and app-specific password
# See: https://support.google.com/accounts/answer/185833
# GMAIL_USER=''
# GMAIL_PASSWORD=''  # Use app password, not regular password

# OPTIONAL - LangChain tracing/debugging
# Uncomment these if you want to enable LangChain tracing
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_ENDPOINT=''
# LANGCHAIN_API_KEY=''
# LANGCHAIN_PROJECT=''

# OPTIONAL - For podcast generation (currently not implemented in main code)
# ELEVENLABS_API_KEY=''
# GOOGLE_API_KEY=''

# OPTIONAL - Alternative LLM providers (for testing/experimentation in notebooks)
# CLAUDE_API_KEY=''
# PERPLEXITY_API_KEY=''